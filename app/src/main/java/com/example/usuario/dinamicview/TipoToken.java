package com.example.usuario.dinamicview;

/**
 * Lista los posibles tokens que seran manejados por el analizador léxico
 */
public enum TipoToken{
    ID,
    AND,
    OR,
    NOT,
    PARIZQ,
    PARDER,
    PESOS
}